# TODO
- Parsing layer
    - Handle escape sequence for `"`, as escaped `"` gets misinterpereted as the end of the string.

- Document layer
    - Check that JsonStrings handle escape sequences and special characters correctly.

- Serialization layer
    - Serialization
    - Deserialization